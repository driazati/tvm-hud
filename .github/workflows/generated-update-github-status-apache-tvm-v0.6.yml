# @generated DO NOT EDIT MANUALLY
# Generated from .github/templates/update_github_status.yml.j2
# @generated DO NOT EDIT MANUALLY
# Generated from .github/templates/deploy_lambda.yml.j2
name: tvm/apache/v0.6

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

concurrency:
  group: tvm/apache/v0.6
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          pip install \
            aiohttp==3.7.4 \
            requests==2.24.0
      - name: Update
        env:
          GH_PAT: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/update_github_status.py --repo 'tvm' --user 'apache' --branch 'v0.6' --fetch_size 4 --history_size 100
      # - name: Setup tmate session
      #   uses: mxschmitt/action-tmate@v3
      - name: Upload data
        run: |
          echo testing >> public/statuses/apache/tvm/v0.8.json
          if git stash | grep -q 'No local changes'; then
            echo no changes made
            exit 0
          fi
          git fetch origin
          git checkout data
          git stash apply
          git add .
          git commit -m"Update commit status data"
          git push
