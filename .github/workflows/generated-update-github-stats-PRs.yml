# @generated DO NOT EDIT MANUALLY
# Generated from .github/templates/update_github_status.yml.j2
# @generated DO NOT EDIT MANUALLY
# Generated from .github/templates/deploy_lambda.yml.j2
name: apache/tvm/PRs

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: apache/tvm/PRs
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          pip install \
            aiohttp==3.7.4 \
            requests==2.24.0
      - name: Update
        env:
          GH_PAT: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eux
          python scripts/update_github_status.py --repo 'tvm' --user 'apache' --prs --fetch_size 4 --history_size 100
          
      # - name: Setup tmate session
      #   uses: mxschmitt/action-tmate@v3
      - name: Upload data
        run: |
          set -eux
          if git status | grep -q 'working tree clean'; then
            echo no changes made
            exit 0
          fi
          rm -rf /tmp/statuses
          cp -r public/statuses /tmp/statuses
          git checkout -- .
          git fetch origin
          git checkout data
          # git stash apply
          rm -rf public/statuses
          mv /tmp/statuses public
          git add .
          git config user.email "email@example.com"
          git config user.name "robot"
          git commit -m"Update commit status data"
          git push
